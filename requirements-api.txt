# Dependências de runtime da API hospedada (ticket 08). Subconjunto de requirements.txt: só o
# que `uvicorn api.main:app` importa de fato — sem streamlit, reportlab, pdfkit etc., que
# servem só o app Streamlit legado e o pdf_generator (nenhum dos dois entra na imagem).
#
# Versões pinadas nas mesmas do ambiente que gerou `models/pas3/manifest.json`, para que o
# pacote LightGBM seja lido pela mesma geração de libs que o treinou (`ambiente` no manifesto).
fastapi==0.136.3
uvicorn[standard]==0.49.0
numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
lightgbm==4.6.0
joblib==1.5.3
